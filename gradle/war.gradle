apply plugin: "war"

bootWar {
    mainClass = "com.recipegrapher.app.RecipeGrapherApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
